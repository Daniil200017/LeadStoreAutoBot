<Window x:Class="LeadStoreAutoBot.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LeadStore Auto Bot"
        Width="420" Height="200"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#1E1E2E">
    <Border CornerRadius="10" Background="#1E1E2E" BorderBrush="#3A3A4E" BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="LeadStore Auto Bot"
                       FontSize="20" FontWeight="Bold" Foreground="White"
                       HorizontalAlignment="Center"/>

            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock x:Name="StatusText" Text="Проверка обновлений..."
                           FontSize="13" Foreground="#CFCFE0"
                           HorizontalAlignment="Center" Margin="0,0,0,12"/>
                <ProgressBar x:Name="Bar" Height="8" Minimum="0" Maximum="100"
                             IsIndeterminate="True" Foreground="#7C5CFF" Background="#2A2A3C"/>
            </StackPanel>

            <TextBlock Grid.Row="2" x:Name="VersionText"
                       FontSize="11" Foreground="#7A7A8C"
                       HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</Window>
