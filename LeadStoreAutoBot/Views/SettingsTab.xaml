<UserControl x:Class="LeadStoreAutoBot.Views.SettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:LeadStoreAutoBot.ViewModels"
             d:DataContext="{d:DesignInstance vm:MainViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer Padding="20" VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="780" HorizontalAlignment="Left">

            <!-- Внешний вид -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource HeaderText}" Text="🎨 Внешний вид" Margin="0,0,0,12"/>

                    <TextBlock Style="{StaticResource MutedText}" Text="Тема"/>
                    <ComboBox ItemsSource="{Binding Themes}"
                              SelectedItem="{Binding SelectedTheme}"
                              DisplayMemberPath="DisplayName"
                              Margin="0,4,0,12"/>

                    <TextBlock Style="{StaticResource MutedText}" Text="Акцентный цвет"/>
                    <ComboBox ItemsSource="{Binding Accents}"
                              SelectedItem="{Binding SelectedAccent}"
                              DisplayMemberPath="DisplayName"
                              Margin="0,4,0,8"/>
                </StackPanel>
            </Border>

            <!-- Звуки -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource HeaderText}" Text="🔔 Звуки" Margin="0,0,0,12"/>
                    <ui:ToggleSwitch Content="Звуковые уведомления" IsChecked="{Binding SoundEnabled}"/>

                    <TextBlock Style="{StaticResource MutedText}" Text="Звук успеха" Margin="0,12,0,4"/>
                    <ComboBox ItemsSource="{Binding SoundDonePresets}"
                              SelectedItem="{Binding SelectedSoundDone}"/>

                    <TextBlock Style="{StaticResource MutedText}" Text="Звук ошибки" Margin="0,8,0,4"/>
                    <ComboBox ItemsSource="{Binding SoundErrorPresets}"
                              SelectedItem="{Binding SelectedSoundError}"/>

                    <TextBlock Style="{StaticResource MutedText}" Text="Громкость" Margin="0,12,0,4"/>
                    <Slider Minimum="0" Maximum="100" Value="{Binding Volume}" TickFrequency="10"/>
                </StackPanel>
            </Border>

            <!-- Поведение -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource HeaderText}" Text="⚙ Поведение" Margin="0,0,0,12"/>
                    <ui:ToggleSwitch Content="Пропускать дубли" IsChecked="{Binding SkipDuplicates}" Margin="0,0,0,8"/>
                    <ui:ToggleSwitch Content="Экспортировать лог" IsChecked="{Binding ExportLog}"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
