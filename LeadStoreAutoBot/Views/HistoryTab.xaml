<UserControl x:Class="LeadStoreAutoBot.Views.HistoryTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:LeadStoreAutoBot.ViewModels"
             d:DataContext="{d:DesignInstance vm:MainViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Поиск -->
        <TextBox Grid.Row="0" Margin="0,0,0,12"
                 Tag="🔍 Поиск по дате, сайту, режиму или деталям..."
                 Text="{Binding HistorySearch, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Двухколоночный layout: список слева, детали справа -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Список сессий -->
            <Border Grid.Column="0" Background="{DynamicResource Bg2Brush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                    CornerRadius="8">
                <DataGrid ItemsSource="{Binding History}"
                          SelectedItem="{Binding SelectedHistory}"
                          AutoGenerateColumns="False"
                          Background="Transparent"
                          Foreground="{DynamicResource TextBrush}"
                          RowBackground="Transparent"
                          AlternatingRowBackground="{DynamicResource Bg3Brush}"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                          BorderThickness="0"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          RowHeight="34"
                          ColumnHeaderHeight="34"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата"     Binding="{Binding Date}"    Width="*"/>
                        <DataGridTextColumn Header="Режим"    Binding="{Binding Mode}"    Width="80"/>
                        <DataGridTextColumn Header="Сайт"     Binding="{Binding Site}"    Width="*"/>
                        <DataGridTextColumn Header="✅"        Binding="{Binding Created}" Width="50"/>
                        <DataGridTextColumn Header="⏭"        Binding="{Binding Skipped}" Width="50"/>
                        <DataGridTextColumn Header="❌"        Binding="{Binding Errors}"  Width="50"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Детали выбранной сессии -->
            <Border Grid.Column="2" Background="{DynamicResource Bg2Brush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                    CornerRadius="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{DynamicResource Bg3Brush}"
                            Padding="12,8" CornerRadius="8,8,0,0">
                        <TextBlock Style="{StaticResource HeaderText}" Text="📋 Подробности сессии"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" Padding="12" VerticalScrollBarVisibility="Auto">
                        <Grid>
                            <!-- Подсказка когда ничего не выбрано -->
                            <TextBlock Text="← Выберите сессию из списка слева чтобы увидеть подробности"
                                       HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource MutedText}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedHistory}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <!-- Контент деталей -->
                            <TextBox Text="{Binding SelectedHistory.Details, Mode=OneWay}"
                                     IsReadOnly="True"
                                     TextWrapping="NoWrap"
                                     FontFamily="Consolas"
                                     FontSize="12"
                                     Foreground="{DynamicResource TextBrush}"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Disabled"
                                     HorizontalScrollBarVisibility="Auto">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedHistory}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                        </Grid>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
